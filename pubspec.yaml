name: flutter_application_helpvrywhere
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.2
  geocoding: ^2.1.1
  cloud_firestore: 6.2.0
  flutter_overlay_window: ^0.4.4

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  firebase_core: ^4.6.0
  flutter_native_splash: ^2.4.7
  http: ^1.2.2 # ← NUEVO: para llamar la API de Gemini
  image_picker: ^1.1.2 # ← NUEVO: para elegir imágenes de la galería
  firebase_auth: ^6.3.0
  speech_to_text: ^7.3.0
  permission_handler: ^11.3.1
  flutter_tts: ^4.2.3
  mapbox_maps_flutter: ^2.5.0 # nearby-requests map UI
  flutter_dotenv: ^5.1.0 # loads tokens from .env at startup
  google_fonts: ^6.2.1 # Plus Jakarta Sans for the nearby-request UI
  shared_preferences: ^2.3.2 # local storage for pre-signup onboarding choices

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

# Generates platform launcher icons from a single source.
# Run after editing:  dart run flutter_launcher_icons
# Android-only for now — iOS icons can be regenerated later if needed.
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/help-vrywhere-logo.png"
  # Adaptive icon (Android API 26+). OS masks the foreground into
  # whatever shape the launcher uses; the surrounding whitespace acts as
  # the safe zone, so the mark scales nicely inside the mask.
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/help-vrywhere-logo.png"

flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/
    - assets/how-to/ask/
    - assets/how-to/offer/
    - assets/how-to/ai/
    - .env
