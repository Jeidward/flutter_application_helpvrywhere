name: flutter_application_helpvrywhere
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.6.0
  flutter_native_splash: ^2.4.7
  http: ^1.2.2           # ← NUEVO: para llamar la API de Gemini
  image_picker: ^1.1.2   # ← NUEVO: para elegir imágenes de la galería

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/